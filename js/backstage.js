/*function read_loginstate_from_cookie(){
    var cookieValue = document.cookie.replace(/(?:(?:^|.*;\s*)loginstate\s*\=\s*([^;]*).*$)|^.*$/, "$1");
    if(!cookieValue){
        window.open("administrator_login","_self");
    }
}

login_state = {
    loginstate: true
}
document.cookie = "login_state="+JSON.stringify(login_state)+"path=/";//保存登录状态。登录过后不需要输入账号密码。
暂不打算做后台
*/